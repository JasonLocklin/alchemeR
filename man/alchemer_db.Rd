% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{alchemer_db}
\alias{alchemer_db}
\title{Open (or create) the alchemeR application database}
\usage{
alchemer_db(db = alchemer_db_path(), read_only = FALSE)
}
\arguments{
\item{db}{Application database directory. Defaults to \code{\link[=alchemer_db_path]{alchemer_db_path()}}.}

\item{read_only}{Attach without creating directories or schema, and without
permission to write -- for analysts who only ever query.}
}
\value{
A \code{DBI} connection with the catalog attached as \code{alchemer}.
}
\description{
Attaches the DuckLake catalog at \code{db} as \code{alchemer}, asserting that its
stamped spec version is at least 1.0 (ADR-001) -- an older \code{duckdb} build
writes the pre-release 0.3 spec silently, and this refuses to write to (or
misread) one. \code{raw}/\code{meta}/\code{pub} schemas and tables are created on first
connect and are safe to re-run.
}
\details{
Any number of processes may attach at once, readers and writers alike, so
an analyst querying the database never blocks a scheduled \code{ingest()} and is
never blocked by one.
}
