% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_surveys.R
\name{alchemer_survey}
\alias{alchemer_survey}
\title{Fetch one survey's full definition tree}
\usage{
alchemer_survey(survey_id, client = alchemer_client())
}
\arguments{
\item{survey_id}{Alchemer survey id.}

\item{client}{An \code{alchemer_client}, from \code{\link[=alchemer_client]{alchemer_client()}}. Defaults to
one built from the environment (\code{ALCHEMER_API_TOKEN}, \code{ALCHEMER_API_SECRET}).}
}
\value{
A one-row tibble. Nested \code{pages}/\code{questions}/\code{options} are kept as
a list-column (\code{pages}) holding the tree exactly as the API returned it.
}
\description{
A single request returns the survey's pages, questions, and options
nested inline (\code{GET /v5/survey/{id}}) — this is \code{alchemeR}'s preferred way
to read a survey's structure, rather than crawling the SurveyPage/
SurveyQuestion/SurveyOption list endpoints separately.
}
