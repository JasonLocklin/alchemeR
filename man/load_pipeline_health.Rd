% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{load_pipeline_health}
\alias{load_pipeline_health}
\title{Load a pipeline-health summary into the analytics database}
\usage{
load_pipeline_health(
  dest_con,
  db = alchemer_db_path(),
  table = "alchemer_pipeline_health",
  schema = NULL
)
}
\arguments{
\item{dest_con}{A \code{DBI} connection to the destination database.}

\item{db}{Application database directory. Defaults to \code{\link[=alchemer_db_path]{alchemer_db_path()}}.}

\item{table}{Destination table name.}

\item{schema}{Destination schema name (e.g. \code{"dbo"} for SQL Server).
\code{NULL} uses the destination's default schema.}
}
\value{
Invisibly, the health row that was loaded (a one-row tibble).
}
\description{
Writes \code{\link[=db_status]{db_status()}} into the destination as a plain table, so tools
monitoring the analytics database can see whether the pipeline is
current and healthy without needing any access back to the local
application database.
}
