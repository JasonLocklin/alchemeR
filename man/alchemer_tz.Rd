% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{alchemer_tz}
\alias{alchemer_tz}
\title{Timezone for the publication layer}
\usage{
alchemer_tz()
}
\value{
A single IANA timezone name, e.g. \code{"America/Toronto"}.
}
\description{
The timezone \code{pub}'s timestamps are presented in, and the timezone
Alchemer's own unsuffixed timestamps are read as. \code{raw} is never
converted (ADR-003) -- it keeps whatever string the API sent.
}
\details{
Defaults to \code{ALCHEMER_TZ}, then to the machine's own timezone
(\code{Sys.timezone()}). \strong{Set it explicitly for a scheduled job}: leaving it
unset makes \code{pub} depend on the timezone of whichever machine last ran
\code{\link[=pub_layer]{pub_layer()}}, so a laptop and a UTC server would write different values
into the same shared database.
}
\keyword{internal}
