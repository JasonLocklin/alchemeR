% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{ingest_failures}
\alias{ingest_failures}
\title{Surveys that are currently failing to refresh, and why}
\usage{
ingest_failures(db = alchemer_db_path())
}
\arguments{
\item{db}{Application database directory. Defaults to \code{\link[=alchemer_db_path]{alchemer_db_path()}}.}
}
\value{
A tibble, one row per currently-failing survey (zero rows when
nothing is failing), ordered worst-first by \code{consecutive_failures}:

\describe{
\item{\code{survey_id}, \code{title}}{Which survey. \code{title} is \code{NA} for a survey
that has never been archived successfully.}
\item{\code{consecutive_failures}}{Failed attempts since the last success.
Climbing across runs means a persistent cause, not a blip.}
\item{\code{last_successful_refresh_at}}{How stale the archived data is --
\code{NA} if this survey has never succeeded. \strong{The data as of this
timestamp is still intact}; a failed refresh never damages it.}
\item{\code{last_attempt_at}, \code{last_error_run_id}, \code{last_error_phase}}{When
it last tried, under which \code{meta.runs} id, and at which phase
(\code{"refresh"}, or \code{"probe"} for a change-detection request that failed
without stopping the refresh).}
\item{\code{http_status}}{The HTTP status when the request itself failed, or
Alchemer's own error code when it answered 200 with
\code{result_ok: false} -- Alchemer's codes are HTTP-shaped (401, 500), so
both read the same way. \code{NA} for a connection failure, which never got
a status, or an integrity failure, which made no request.}
\item{\code{message}}{The full error, including the redacted request path.
\code{vignette("troubleshooting")} decodes the recurring shapes.}
\item{\code{failed_checks}}{Integrity assertions that failed on that attempt
(ADR-006), \code{check_name: message} and semicolon-separated. Populated
only when the failure \emph{was} an integrity failure.}
}
}
\description{
The diagnostic companion to \code{\link[=db_status]{db_status()}}, which reports only that the last
run \emph{had} failures. This names them: one row per survey whose most recent
\code{\link[=ingest]{ingest()}} attempt failed, carrying the error message, the status code, and
any failed integrity checks from that same attempt.
}
\details{
Reads \code{meta.survey_state} -- written outside the refresh transaction
precisely so a failure's bookkeeping survives its own rollback (ADR-007) --
joined to the matching \code{meta.run_events} and \code{meta.integrity_checks} rows.
Those tables remain the complete log; this is the standing question asked of
them. Notably, a survey listed here is \emph{failing now}: it drops off by itself
on the next successful refresh.

A survey whose probe fails but whose refresh then succeeds is \strong{not} listed,
because it is not failing -- change detection just fell back to refreshing it
anyway. Those events are in \code{meta.run_events} with \code{phase = 'probe'}.
}
\seealso{
\code{\link[=db_status]{db_status()}} for the run-level summary, \code{\link[=db_check]{db_check()}} to re-run the
integrity assertions account-wide.
}
