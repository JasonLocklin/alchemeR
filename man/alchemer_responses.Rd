% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_surveys.R
\name{alchemer_responses}
\alias{alchemer_responses}
\title{List a survey's responses}
\usage{
alchemer_responses(survey_id, client = alchemer_client(), ...)
}
\arguments{
\item{survey_id}{Alchemer survey id.}

\item{client}{An \code{alchemer_client}, from \code{\link[=alchemer_client]{alchemer_client()}}. Defaults to
one built from the environment (\code{ALCHEMER_API_TOKEN}, \code{ALCHEMER_API_SECRET}).}

\item{...}{Additional query parameters (e.g. \code{order_by}, \code{resultsperpage},
or \code{`filter[field][0]`} style filters) passed through to the API.}
}
\value{
A tibble, one row per response.
}
\description{
Downloads \strong{all} responses (paginated at 500/page), not a filtered
subset — matching \code{alchemeR}'s full-refresh design (ADR-004). Pass
Alchemer's documented filter/order_by query parameters via \code{...} to
narrow the request, e.g. \code{order_by = "-date_updated"}.
}
