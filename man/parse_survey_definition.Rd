% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{parse_survey_definition}
\alias{parse_survey_definition}
\title{Parse one survey's full definition tree into pages/questions/options}
\usage{
parse_survey_definition(survey_id, def, varnames = character(0))
}
\arguments{
\item{survey_id}{Survey id (kept alongside \code{def$id} in case of mismatch).}

\item{def}{The parsed definition tree.}

\item{varnames}{Named character vector, \code{question_id -> varname}.}
}
\value{
A list with \code{definition}, \code{pages}, \code{questions}, \code{options} tibbles.
}
\description{
\code{def} is the raw result of \code{alchemer_fetch(client, "survey/{id}")} --
pages, questions, and options nested inline. \code{varname} is absent from
this tree (it is only returned by the separate \code{surveyquestion} list, per
ADR discussion in plan.md), so it is merged in from \code{varnames}, a named
character vector \code{question_id -> varname} built by the caller from that
endpoint.
}
\keyword{internal}
